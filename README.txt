pytest
    testing tool

tox
    run test suite in multiple environments
    tox will throw 'ERROR: InvocationError:' if there are no tests available

import modules
    wrapping up code into a package
    python setup.py sdist
